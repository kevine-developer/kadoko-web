---
import LegalLayout from "../layouts/LegalLayout.astro";
import { marked } from "marked";

const apiUrl = import.meta.env.PUBLIC_API_URL || "http://localhost:3000";
const response = await fetch(`${apiUrl}/api/config/legal/LEGAL_NOTICE`).catch(() => null);
const data = response ? await response.json().catch(() => null) : null;

const title = `Mentions <span class="italic text-coolgrey">Légales.</span>`;
const metaTitle = `Mentions Légales | Kadô-kou`;
const description =
  "Mentions légales de l'application Kadô-kou. Découvrez les informations sur l'éditeur, l'hébergeur et la propriété intellectuelle de la plateforme.";

let contentHtml = "";
let lastUpdated = "Non publié";

if (data?.success && data?.document) {
  contentHtml = await marked.parse(data.document.content);
  const date = new Date(data.document.updatedAt);
  lastUpdated = date.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' });
} else {
  contentHtml = "<p>Les Mentions Légales sont en cours de mise à jour.</p>";
}
---

<LegalLayout
  title={title}
  metaTitle={metaTitle}
  description={description}
  subtitle="Éditeur & Hébergement"
  lastUpdated={lastUpdated}
>
  <div class="prose prose-sm md:prose-base prose-gray max-w-none text-deepblack legal-content" set:html={contentHtml} />
</LegalLayout>

<style is:global>
  .legal-content h1, .legal-content h2, .legal-content h3 {
    color: #1A1A1A;
    margin-top: 2rem;
    margin-bottom: 1rem;
    font-weight: bold;
  }
  .legal-content h2 {
    font-size: 1.5rem;
  }
  .legal-content p {
    margin-bottom: 1rem;
    line-height: 1.7;
  }
  .legal-content ul {
    list-style-type: disc;
    margin-left: 1.5rem;
    margin-bottom: 1rem;
  }
  .legal-content a {
    color: #AF9062;
    text-decoration: underline;
  }
</style>
