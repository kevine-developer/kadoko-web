---
import LegalLayout from "../layouts/LegalLayout.astro";
import { marked } from "marked";

const apiUrl = import.meta.env.PUBLIC_API_URL || "http://localhost:3000";
const response = await fetch(`${apiUrl}/api/config/legal/PRIVACY_POLICY`).catch(() => null);
const data = response ? await response.json().catch(() => null) : null;

const title = `Politique de <span class="italic text-coolgrey">Confidentialité.</span>`;
const metaTitle = `Politique de Confidentialité | Kadô-kou`;
const description =
  "Politique de confidentialité de Kadô-kou. Découvrez comment nous protégeons vos données personnelles, gérons vos wishlists et respectons votre vie privée (RGPD).";

let contentHtml = "";
let lastUpdated = "Non publié";

if (data?.success && data?.document) {
  contentHtml = await marked.parse(data.document.content);
  const date = new Date(data.document.updatedAt);
  lastUpdated = date.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' });
} else {
  contentHtml = "<p>La Politique de Confidentialité est en cours de mise à jour.</p>";
}
---

<LegalLayout
  title={title}
  metaTitle={metaTitle}
  description={description}
  subtitle="Protection des Données"
  lastUpdated={lastUpdated}
>
  <div class="prose prose-sm md:prose-base prose-gray max-w-none text-deepblack legal-content" set:html={contentHtml} />
</LegalLayout>

<style is:global>
  .legal-content h1, .legal-content h2, .legal-content h3 {
    color: #1A1A1A;
    margin-top: 2rem;
    margin-bottom: 1rem;
    font-weight: bold;
  }
  .legal-content h2 {
    font-size: 1.5rem;
  }
  .legal-content p {
    margin-bottom: 1rem;
    line-height: 1.7;
  }
  .legal-content ul {
    list-style-type: disc;
    margin-left: 1.5rem;
    margin-bottom: 1rem;
  }
  .legal-content a {
    color: #AF9062;
    text-decoration: underline;
  }
</style>
